<tr data-id="<%= event.slug %>" data-type="<%= event.type %>"<% if (variant === 'overview') { %> data-featured="<%= event.featured %>"<% } %>>
  <td>
    <div class="cell-media">
      <img src="<%= event.image %>" alt="<%= event.title %>" class="cell-thumb" />
      <div class="cell-info"><span class="cell-title"><%= event.title %></span></div>
    </div>
  </td>
  <td>
    <span class="status-badge <%= event.type === 'official' ? 'badge-official' : 'badge-submission' %>">
      <%= event.type === 'official' ? 'Official' : 'Submission' %>
    </span>
  </td>
  <% if (variant === 'full') { %>
    <td><%= event.category %></td>
    <td><%= event.location %></td>
    <td><%= event.date %></td>
    <td>
      <span class="status-badge <%= event.status === 'approved' ? 'badge-published' : 'badge-pending' %>">
        <%= event.status === 'approved' ? 'Published' : 'Pending' %>
      </span>
    </td>
  <% } else { %>
    <td>
      <span class="status-badge <%= event.status === 'approved' ? 'badge-published' : 'badge-pending' %>">
        <%= event.status === 'approved' ? 'Published' : 'Pending' %>
      </span>
    </td>
    <td><%= event.date %></td>
  <% } %>
  <td>
    <div class="action-group">
      <% if (variant === 'full' && event.type !== 'official' && event.status === 'pending') { %>
        <button class="btn btn-secondary btn-sm" data-action="review-submission" data-slug="<%= event.slug %>">Review</button>
      <% } else { %>
        <button class="action-btn" title="Edit Event" data-action="edit-event" data-slug="<%= event.slug %>">
          <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/></svg>
        </button>
        <% if (variant === 'overview') { %>
          <button class="action-btn" title="View Event Page" data-action="view-event-page">
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
          </button>
        <% } %>
        <button class="action-btn delete" title="Delete Event" data-action="delete-event" data-slug="<%= event.slug %>" data-name="<%= event.title %>">
          <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/></svg>
        </button>
      <% } %>
    </div>
  </td>
</tr>